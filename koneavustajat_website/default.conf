server {
  listen 80;
  server_name localhost;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    # If the requested file exists, serve it; otherwise, serve index.html.
    try_files $uri /index.html;
  }

  # Optionally, you can add other settings for caching, security, etc.
}
